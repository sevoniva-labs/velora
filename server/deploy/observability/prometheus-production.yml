global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: velora
    scheme: https
    metrics_path: /metrics
    tls_config:
      ca_file: /etc/velora-tls/ca.pem
      cert_file: /etc/velora-tls/client.pem
      key_file: /etc/velora-tls/client-key.pem
      server_name: velora.velora.svc.cluster.local
      insecure_skip_verify: false
    static_configs:
      - targets: ["velora.velora.svc.cluster.local:8080"]
