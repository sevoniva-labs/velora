# HSM / KMS / 密码机 Adapter Slot

金融生产环境建议将主密钥、签名私钥和高敏凭据托管到 KMS/HSM/密码机，不把长期主密钥直接保存在应用环境变量中。

Forge 已提供 `secrets.Provider` 与 `crypto.Provider` 边界。厂商适配器应负责：身份认证、密钥标识/版本、轮换、双人复核流程配合、审计、故障降级策略和国密算法能力。任何厂商密码机接入均需结合实际密码产品型号和商用密码应用安全性评估要求验证。
